<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bookai.mapper.BookChapterMapper">

    <select id="selectByBookId" resultType="com.bookai.entity.BookChapter">
        SELECT id, book_id, title, chapter_num, is_free, create_time, update_time
        FROM book_chapter
        WHERE book_id = #{bookId}
        ORDER BY chapter_num ASC
    </select>

    <select id="selectByBookIdAndChapterNum" resultType="com.bookai.entity.BookChapter">
        SELECT *
        FROM book_chapter
        WHERE book_id = #{bookId} AND chapter_num = #{chapterNum}
    </select>

</mapper>
