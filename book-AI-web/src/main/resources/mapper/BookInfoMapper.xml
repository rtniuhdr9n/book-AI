<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bookai.mapper.BookInfoMapper">

    <select id="selectPageByCategory" resultType="com.bookai.entity.BookInfo">
        SELECT *
        FROM book_info
        WHERE is_on_sale = 1
        <if test="categoryId != null">
            AND category_id = #{categoryId}
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="selectHotBooks" resultType="com.bookai.entity.BookInfo">
        SELECT *
        FROM book_info
        WHERE is_on_sale = 1
        ORDER BY sales DESC
        LIMIT #{limit}
    </select>

    <select id="selectByIds" resultType="com.bookai.entity.BookInfo">
        SELECT *
        FROM book_info
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <update id="decreaseStock">
        UPDATE book_info
        SET stock = stock - #{quantity}
        WHERE id = #{bookId} AND stock >= #{quantity}
    </update>

    <update id="increaseSales">
        UPDATE book_info
        SET sales = sales + #{quantity}
        WHERE id = #{bookId}
    </update>

</mapper>
